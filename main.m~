clear all;
%% set paramters
pose_fixed = [];
pose_transit = 


%%
% path = minimum_snap_generator();
path = unconstrained_qp_generator();

% run_trajectory(path);



    plot(path(:,1),path(:,2));
    grid on;